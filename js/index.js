import MythologyApp from "./classes/mythologyApp.mjs";

// Initialize the application when the document is ready
document.addEventListener('DOMContentLoaded', () => {
    const app = new MythologyApp();
});